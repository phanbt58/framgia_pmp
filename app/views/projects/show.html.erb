<%= render "shared/category_tab", project: @project,
  active_tab: Settings.tabs.overview_project, f: nil %>
<div id="projects">
  <div class="subject-content">
    <div class="row">
      <div class="col-md-9 project-description">
        <h3><%= @project.name %></h3>
        <p><i><%= @project.description %></i></p>
        <p class="label"><%= t "projects.show.start_date" %></p>
        <p><%= @project.start_date %></p>
        <p class="label"><%= t "projects.show.end_date" %></p>
        <p><%= @project.end_date %></p>
      </div>
      <div class="col-md-3 box">
        <p class="label"><%= t "project.show.manager" %></p>
        <p class="list-user">
          <% @project.managers.each_with_index do |manager, index| %>
            <%= link_to manager.user_name, "#" %>
            <%= (index == @project.managers.size - 1)  ? "." : "," %>
          <% end %>
        </p>
        <p class="label"><%= t "project.show.member" %></p>
        <p class="list-user">
          <% @project.developers.each_with_index do |developer, index| %>
            <%= link_to developer.user_name, "#" %>
            <%= (index == @project.developers.size - 1)  ? "." : "," %>
          <% end %>
        </p>
      </div>
    </div>
  </div>
</div>
