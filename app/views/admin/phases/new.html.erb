<div id="phases">
  <h2 class="text-center"><%= t "phases.new" %></h2>
  <%= form_for @phase, url: admin_project_sprint_phases_path(@project, @sprint) do |f| %>
    <%= render "form", f: f %>
  <% end %>
</div>

